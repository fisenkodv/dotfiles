- defaults:
    shell:
      stdout: true
      stderr: true
      quiet: true

- link:
    ~/.config/git/config.local: config/git/config.linux

- shell:
    - command: ./scripts/install-linux-packages.sh
      description: 📦 Installing apt packages and tools
    - command: mise install
      description: 🛠️ Installing mise tools
