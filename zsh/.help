function aliases() {
  bold=$(tput bold)
  normal=$(tput sgr0)

  echo "Aliases:"
  echo "  ${bold}..${normal} - Easier navigation";
  echo "  ${bold}...${normal} - Easier navigation";
  echo "  ${bold}....${normal} - Easier navigation";
  echo "  ${bold}.....${normal} - Easier navigation";
  echo "  ${bold}~${normal} - Easier navigation";
  echo "  ${bold}-${normal} - Easier navigation";
  echo "  ${bold}dl${normal} - Shortcut: Download";
  echo "  ${bold}dt${normal} - Shortcut: Desktop";
  echo "  ${bold}p${normal} - Shortcut: Projects";
  echo "  ${bold}g${normal} - Shortcut: git";
  echo "  ${bold}gc${normal} - Shortcut: git commit";
  echo "  ${bold}gp${normal} - Shortcut: git push";
  echo "  ${bold}h${normal} - Shortcut: history";
  echo "  ${bold}l${normal} - List all files colorized in long format";
  echo "  ${bold}ll${normal} - List all files colorized in long format";
  echo "  ${bold}la${normal} - List all files colorized in long format, including dot files";
  echo "  ${bold}lsd${normal} - List only directories";
  echo "  ${bold}ls${normal} - Always use color output for 'ls'";
  echo "  ${bold}sudo${normal} - Enable aliases to be sudo’ed";
  echo "  ${bold}gurl${normal} - Gzip-enabled 'curl'";
  echo "  ${bold}week${normal} - Get week number";
  echo "  ${bold}timer${normal} - Stopwatch";
  echo "  ${bold}update${normal} - Get OS X Software Updates, and update installed Ruby gems, Homebrew, npm, and their installed packages";
  echo "  ${bold}ip${normal} - IP addresses";
  echo "  ${bold}ips${normal} - IP addresses";
  echo "  ${bold}whois${normal} - Enhanced WHOIS lookups";
  echo "  ${bold}flush${normal} - Flush Directory Service cache";
  echo "  ${bold}lscleanup${normal} - Clean up LaunchServices to remove duplicates in the “Open With” menu";
  echo "  ${bold}sniff${normal} - View HTTP traffic";
  echo "  ${bold}httpdump${normal} -  ";
  echo "  ${bold}hd${normal} - Canonical hex dump; some systems have this symlinked";
  echo "  ${bold}md5sum${normal} - OS X has no 'md5sum', so use 'md5' as a fallback";
  echo "  ${bold}sha1sum${normal} - OS X has no 'sha1sum', so use 'shasum' as a fallback";
  echo "  ${bold}c${normal} - Trim new lines and copy to clipboard";
  echo "  ${bold}cleanup${normal} - Recursively delete '.DS_Store' files";
  echo "  ${bold}rot13${normal} - ROT13-encode text. Works for decoding, too!";
  echo "  ${bold}emptytrash${normal} - Empty the Trash on all mounted volumes and the main HDD";
  echo "  ${bold}show${normal} - Show hidden files in Finder";
  echo "  ${bold}hide${normal} - Hide hidden files in Finder";
  echo "  ${bold}hidedesktop${normal} - Hide all desktop icons (useful when presenting)";
  echo "  ${bold}showdesktop${normal} - Show all desktop icons (useful when presenting)";
  echo "  ${bold}urlencode${normal} - URL-encode strings";
  echo "  ${bold}mergepdf${normal} - Merge PDF files, 'mergepdf -o output.pdf input{1,2,3}.pdf'";
  echo "  ${bold}gm${normal} - Merge conflict";
  echo "  ${bold}spotoff${normal} - Disable Spotlight";
  echo "  ${bold}spoton${normal} - Enable Spotlight";
  echo "  ${bold}badge${normal} - Ring the terminal bell, and put a badge on Terminal.app’s Dock icon";
  echo "  ${bold}map${normal} - Intuitive map function. For example, to list all directories that contain a certain file: find . -name .gitattributes | map dirname";
  echo "  ${bold}chromekill${normal} - Kill all the tabs in Chrome to free up memory";
  echo "  ${bold}poweroff${normal} - Power off";
}

function functions() {
  bold=$(tput bold)
  normal=$(tput sgr0)

  echo "Functions:"
  echo "  ${bold}listening${normal} - Print open ports";
  echo "  ${bold}mkd${normal} - Create a new directory and enter it ";
  echo "  ${bold}fs${normal} - Determine size of a file or total size of a directory ";
  echo "  ${bold}dataurl${normal} - Create a data URL from a file ";
  echo "  ${bold}dataurlc${normal} - Create a data URL from a file and copy it to the clipboard";
  echo "  ${bold}server${normal} - Start an HTTP server from a directory, optionally specifying the port ";
  echo "  ${bold}hr${normal} - Print line";
  echo "  ${bold}tre${normal} - 'tre' is a shorthand for 'tree' with hidden files and color enabled, ignoring the '.git' directory, listing directories first. The output gets piped into 'less' with options to preserve color and line numbers, unless the output is small enough for one screen";
  echo "  ${bold}clone${normal} - Git clone";
  echo "  ${bold}gt${normal} - Git graph log";
}